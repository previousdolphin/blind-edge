{
  "name": "blind-edge",
  "version": "1.5.0",
  "description": "B.E.Chat — end-to-end encrypted, local-first messaging PWA with a zero-knowledge relay. No accounts, no runtime dependencies.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/previousdolphin/blind-edge.git"
  },
  "homepage": "https://blind-edge.pages.dev",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npx serve public -p 8080",
    "test": "node --test test/*.test.mjs",
    "db:create": "cd worker && npx wrangler d1 create blind-edge-db",
    "db:init": "cd worker && npx wrangler d1 execute blind-edge-db --file=schema.sql",
    "worker:dev": "cd worker && npx wrangler dev",
    "worker:deploy": "cd worker && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "serve": "^14.0.0"
  }
}
